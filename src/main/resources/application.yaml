mybatis:
  mapper-locations: classpath:mappers/**/*.xml
server:
  port: 8000
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?allowMultiQueries=true&serverTimezone=UTC&useUniCode=yes&characterEncoding=UTF-8
    username: myid
    password: 1234
  servlet:
    multipart:
      maxFileSize: 100MB
      maxRequestSize: 100MB